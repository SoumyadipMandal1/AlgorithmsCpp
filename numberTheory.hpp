#ifndef MATH_H
#define MATH_H

#include <vector>

void swap(int &, int &);

int gcd(int, int);

int horner(std::vector<int>, int, int);

int factorial(int n);

int permutation(int n, int k);

int combination(int n, int k);

bool isPrime(int n);

#endif
